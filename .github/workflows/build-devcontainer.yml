name: Build Devcontainer

on:
  push:
    paths:
      - '.devcontainer/'
  pull_request:
    paths:
      - '.devcontainer/'
  workflow_dispatch:
    inputs:
      PREBUILD_IMG:
        description: 'Docker image'
        required: false
        default: 'vsc-soroban-examples-prebuild'

jobs:
  verify-scripts:
    name: Shellcheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2
        with:
          sparse-checkout: |
            .devcontainer
      - name: Run Shellcheck
        env:
          SHELLCHECK_OPTS: -s bash
        shell: bash
        run: |
          shellcheck ".devcontainer/ps_autocomplete.sh"
          shellcheck ".devcontainer/pa_welcome.sh"
          shellcheck ".devcontainer/pa_stellar_accounts.sh"

  prebuild:
    name: Docker Build
    runs-on: ubuntu-latest
    environment: devcontainer
    env:
      username: ${{ secrets.DOCKERHUB_USERNAME }}
      password: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          ecr: 'false'
          logout: 'false'
      - name: Checkout
        uses: actions/checkout@v4.2.2
        with:
          clean: 'true'
          path: '.'
      - name: Set up QEMU for multi-architecture builds
        uses: docker/setup-qemu-action@v3.6.0
      - name: Setup Docker buildx for multi-architecture builds
        uses: docker/setup-buildx-action@v3.10.0
        with:
          use: true
          cache-binary: 'true'
      - name: Pre-build Devcontainer
        uses: devcontainers/ci@v0.3.1900000417
        with:
          imageName: ${{env.username}}/vsc-soroban-examples-prebuild
          cacheFrom: ${{env.username}}/vsc-soroban-examples-prebuild
          push: always
          checkoutPath: ${{ github.workspace }}
          platform: linux/amd64,linux/arm64



